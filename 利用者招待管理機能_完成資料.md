# Pharmacy OS 利用者招待・管理機能 完成資料（クロード→チャッピー・フリちゃん）

作成時点：2026年9月2日

文書③（利用者招待・管理機能）の実装が完了しました。ステップ1（文書②の4点）に続く、ステップ2の成果物です。

---

## 1. 変更・新規ファイル一覧

### 新規ファイル

| ファイル | 役割 |
|---|---|
| `invite.html`／`invite.js` | 招待URLを受け取った人が開く、参加専用ページ。Googleログイン→自動登録。LINE内ブラウザ検知・案内表示あり |
| `admin.html`／`admin.js` | 管理者専用の「利用者管理・招待」画面。アカウント枠管理、招待発行、利用者の一時停止・再開・削除 |

### 変更ファイル

| ファイル | 変更内容 |
|---|---|
| `code.gs` | 利用者DB（Googleスプレッドシート）関連の全機能を追加。`verifyIdToken`を拡張し、既存の`ALLOWED_EMAILS`を壊さず新方式へ移行 |

`auth.js`・`config.js`・その他既存ファイルは**変更していません**（既存の日次・月次・年度実績・ステータス・Notion連携、既存のGAS URL、既存の画面デザインには一切手を入れていません）。

---

## 2. 設計の要点

### データ保存先：Googleスプレッドシート

既存のNotion DB構造は変更しない指示だったため、GASから直接・高速に扱える**新規のGoogleスプレッドシート**（`Pharmacy OS 利用者DB`）を1つ自動作成し、その中に`Users`シートと`Invites`シートを持たせています。スプレッドシートは、初回アクセス時に自動的に作成され、そのIDはスクリプトプロパティ`USER_DB_SPREADSHEET_ID`に自動保存されます（手動設定不要）。

**保存している情報**

`Users`シート：Googleアカウントの一意ID（sub）、メールアドレス、権限（admin/employee）、状態（active/suspended）、登録日時、登録方法（existing/group/individual）、メール確認済み状態、最終利用日時、管理者確認済みフラグ

`Invites`シート：招待ID、**招待トークンのハッシュ値**（生のトークンは保存していません）、種類（group/individual）、状態、発行者、発行日時、有効期限、使用回数、最大使用回数

**GoogleのIDトークン自体は、どこにも保存していません。**

### `ALLOWED_EMAILS`を壊さない移行方法

`verifyIdToken`は、まずUsersシートを確認します。そこに登録がなければ、従来通り`ALLOWED_EMAILS`（スクリプトプロパティ）を確認し、該当すればアクセスを許可した上で、**自動的にUsersシートへ管理者として登録**します。これにより、今の管理者（フリちゃん）は、今回の変更後に初めてログインした時点で自動的にUsersシートに移行され、締め出されることはありません。

### 認証の3段階

1. **通常操作**：Googleトークンが正しい＋Usersシートで許可された利用者（active）である＋利用停止でない、をすべて確認
2. **招待URLからの初回登録**：Googleトークンの本人確認（署名・発行元・期限・クライアントID・メール確認済み）のみ先に行い、この時点では許可リスト未登録でも通過させる。その後、招待トークンの有効性とアカウント枠を確認し、条件を満たした場合だけ登録する
3. **管理者操作**：全ての管理者用API（`adminListUsers`等）は、リクエストごとにGAS側で「ログイン済み」かつ「role=admin」かを再確認する。フロント側で管理画面を出す・出さないを判定しているのは見た目だけで、**実際の権限チェックはすべてGAS側で行っている**ため、管理者以外が直接APIを叩いても拒否される

### 同時登録時の排他制御

招待URLからの登録処理は`LockService.getScriptLock()`で排他制御しており、複数人が同時に同じグループ招待から登録しようとしても、アカウント枠の上限を超えて登録されることはありません。

### 招待URLの仕様

- URLには**招待トークンのみ**を含み、GoogleのIDトークンは一切含まれません（`invite.html?token=xxxxx`という形式）
- 招待トークンは、GASの`Utilities.getUuid()`を2つ連結して生成（256ビット相当のランダム性があり、推測は現実的に不可能です）
- グループ招待：発行時に「登録可能人数」「有効日数」を管理者が指定可能。上限に達するか期限切れで自動的に使えなくなる
- 個別招待：最初にログインした1人だけが登録され、その時点で即座に無効化される
- 管理者は、招待一覧画面からいつでも招待を手動で無効化できる

### LINE内ブラウザ対応

`invite.html`は、User-Agentから LINE内ブラウザを検知した場合、目立つ黄色い注意書きを表示し、「他のアプリで開く」「Chromeで開く」等の外部ブラウザへの切り替えを促します。**LINE内ブラウザを技術的に突破しようとする実装はしていません**（指示通り）。

---

## 3. 初回セットアップ手順

今回の機能追加にあたり、**スクリプトプロパティへの新規追加は不要です**（`USER_DB_SPREADSHEET_ID`は初回アクセス時に自動作成・自動保存されます）。

**ただし、以下の手順で初回動作確認をしてください。**

1. `code.gs`をGASにコピー&ペースト→保存→デプロイし直す
2. `invite.html`・`invite.js`・`admin.html`・`admin.js`をGitHubに新規アップロード
3. ブラウザで`admin.html`を開き、フリちゃんのGoogleアカウントでログインする
   - 初回ログイン時に、自動的にUsersシートへ「管理者」として登録されます（前述の移行処理）
   - この時点で、Googleドライブに「Pharmacy OS 利用者DB」という新しいスプレッドシートが自動作成されているはずです（確認してみてください）
4. 管理画面が正しく開けば（「利用者管理・招待」の画面が表示されれば）、初期セットアップは完了です
5. 「アカウント枠」の上限（デフォルト10）を、実際の従業員数に合わせて変更しておくことをおすすめします

**`admin.html`・`invite.html`は、下部ナビには追加していません**（既存の画面デザインを変更しない指示のため）。`admin.html`は、フリちゃんがブックマークして直接開く形になります。

---

## 4. 実機確認手順

| # | 確認内容 | 手順 |
|---|---|---|
| 1 | 管理者以外は管理画面を開けない | 一般スタッフのアカウントで`admin.html`を開き、「この画面は管理者のみ利用できます」と表示されるか確認 |
| 2 | 管理者以外は管理APIを直接呼んでも拒否される | （チャッピー側で、開発者ツール等から直接`adminListUsers`等を呼んで拒否されるか確認） |
| 3 | グループ招待から複数人が参加できる | 管理画面で「グループ招待（登録可能人数：2〜3人）」を発行し、URLを2〜3人で開いて登録できるか確認 |
| 4 | 上限到達後は追加登録できない | ③で人数上限まで使い切った後、もう1人が同じURLで登録を試み、失敗するか確認 |
| 5 | 個別招待は1人だけ登録できる | 「個別招待」を発行し、1人目が登録成功、2人目（同じURL）が失敗するか確認 |
| 6 | 使用済みURLは再利用できない | ⑤の個別招待URLを、登録済みの本人以外がもう一度開いて失敗するか確認（本人が再度開いた場合は「すでに登録済みです」と表示） |
| 7 | 期限切れ・無効化済みURLは利用できない | 管理画面で招待を「無効化」した後、そのURLで登録を試み、失敗するか確認 |
| 8 | 削除すると1枠空く | 利用者を削除→アカウント枠の「残り」が1増えるか確認 |
| 9 | 一時停止では枠が空かない | 利用者を一時停止→アカウント枠の「使用中」が変わらないか確認 |
| 10 | 利用停止者は既存の保存URLを直接呼んでも利用できない | 一時停止した利用者のアカウントで、通常のPharmacy OS画面（例：`home.html`）を開き、ログインが拒否されるか確認 |
| 11 | 同時登録でも上限を超えない | （複数端末で同じグループ招待URLを同時に開いて登録を試す。手動での完全な同時実行確認は難しいため、コードレビューでのLockService確認をもって代替可） |
| 12 | 招待URLにGoogle IDトークンが含まれない | 発行された招待URLを見て、`token=`パラメータ以外に何も含まれていないか確認 |
| 13 | 通常の日次入力と日次編集の両方で確認印を選択できる | ステップ1で確認済み（今回変更なし） |
| 14 | 既存の日次・月次・年度実績・ステータス機能が動く | 一通り操作してみて、これまで通り動くか確認 |
| 15 | Chrome・Samsungブラウザで参加できる | それぞれのブラウザで招待URLを開き、登録できるか確認 |
| 16 | LINE内では外部ブラウザ案内が表示される | LINEのトーク画面で招待URLのリンクをタップして開き、黄色い注意書きが出るか確認 |

---

## 5. 今回変更していないもの（指示通り）

- 既存のNotion DB構造
- 日次・月次・年度実績の計算仕様
- ステータスの期限判定
- 既存の画面デザイン全体（下部ナビ等、既存ページのUIは無変更）
- 既存のGAS URL
- **薬剤師名簿全体の一覧・編集の暫定ブロック**（指示通り、今回は解除していません。招待機能の実機確認が完了してから、別途判断してください）

---

以上。実機確認後、チャッピーに再レビューを依頼してください。
