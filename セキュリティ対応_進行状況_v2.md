# Pharmacy OS セキュリティ対応 進行状況（クロード→チャッピー）※更新版

対象：あおい薬局「Pharmacy OS」アプリのセキュリティ強化作業
作成時点：2026年9月2日（前回メモから更新）

**このメモは、前回の「セキュリティ対応_進行状況.md」を上書きする最新版です。前回メモには「Googleログイン未着手」と書いていましたが、実際にはすでに実装済みです。混乱を避けるため、このメモの内容を正としてください。**

アプリ本体の機能・ファイル構成については、別途「クロードOS引き継ぎメモ_最新版.md」を参照してください。このメモは**セキュリティ対応の進捗専用**です。

---

## 経緯

チャッピーの一次診断→対応→チャッピーの再レビュー、を繰り返しながら進めています。当初の優先順位：

1. NotionページIDの所属DB照合
2. エラー情報の隠蔽
3. 入力値・文字数の制限
4. Googleログインと許可メール一覧
5. 操作履歴
6. 連続アクセス制限・同時保存対策

①〜④は一通り実装が完了し、その後**チャッピーの再レビューで5つの追加指摘**を受け、それも対応済みです（詳細は下記）。

---

## ①〜④：完了（チャッピー再レビュー後の修正込み）

### ① NotionページIDの所属DB照合（完了・チャッピー確認済み）

`assertPageBelongsToDatabase`共通関数で、更新・削除の直前に対象ページの親DBを検証。20箇所すべてに適用済み。処方箋集中率等の削除時の`category`送信も含めて対応済み。

### ② エラー情報の隠蔽（完了・チャッピー確認済み）

全31箇所＋日次業務登録箇所で、Notionの生エラーを利用者に返さない構造に変更済み。詳細は`console.error()`でGAS内部ログにのみ残す。

### ③ 入力値・文字数の制限（完了・再レビューで指摘を受けて強化）

**チャッピーの再レビュー指摘**：「文字数超過を黙って切り詰める」「異常な数値をエラーではなく空欄/0にする」「`2026-99-99`のような実在しない日付が形式チェックだけでは通ってしまう」「マイナス値・極端に大きい値を防げない」

**対応内容**：`sanitizeText`・`sanitizeNumber`・`sanitizeDateValue`・`sanitizeMonthKey`の4関数を、**「黙って丸める」から「異常なら明確にエラーを投げて保存を止める」**方式に全面変更。

- `sanitizeDateValue`：正規表現の形式チェックに加え、実際に`Date`オブジェクトを生成して年月日が一致するか検証（`2026-99-99`のような架空の日付は「実在しない日付です」として拒否）
- `sanitizeNumber`：主要な数値項目（処方箋枚数、後発品調剤率の各数量、割合系、処方箋集中率の受付回数・全体割合、処方箋調べの各科枚数）に、`min`/`max`の範囲チェックを追加（割合系は0〜100、枚数系は0以上など）

### ④ Googleログインと許可メール一覧（完了・チャッピー再レビューで4点の追加指摘→対応済み）

**基本実装**：GAS側`verifyIdToken`関数が、GoogleのtokeninfoエンドポイントでIDトークンの署名・発行元・有効期限・クライアントID・許可メールを検証。`doGet`・`doPost`両方の入口に組み込み。`GOOGLE_CLIENT_ID`・`ALLOWED_EMAILS`はGASのスクリプトプロパティで管理（コード埋め込みなし）。フロント側は`auth.js`（新規共通ファイル）でログイン画面・トークン管理を一元化。

**チャッピー再レビューでの指摘と対応（今回の更新分）**：

1. **「ログイントークンがURLに入っている」** → **対応済み**。以前は`?action=xxx&idToken=...`という形でGETリクエストに埋め込んでいたため、ブラウザ履歴等に残るリスクがあった。**読み取り系8アクション（home/monthly/reminders/status/dayType/dailyReports/yearlyPerformance/pharmacistNames）もすべて`doPost`（POST）経由で受け付けるようGAS側を拡張**し、フロント側は`auth.js`に新設した`authFetch()`共通関数で、全アクションをPOSTのJSON本文でidTokenを送る方式に統一。全JSファイルを対象に、URLにidTokenが残っていないことを`grep`で確認済み

2. **「毎回Googleへ本人確認しに行っていて重い」** → **対応済み**。`verifyIdToken`に`CacheService`によるキャッシュを追加。同じトークンでの検証結果を5分間再利用し、Googleのtokeninfoエンドポイントへの問い合わせ回数を削減

3. **入力制限の未完成分** → 上記③で対応済み（同じ指摘が③にも③にもまたがっていたため統合して対応）

4. **「薬剤師名がまだGitHubで公開中（config.js）」** → **対応済み**。`config.js`から`PHARMACISTS`配列を削除。代わりに、**認証済みユーザーのみアクセスできる新しいGASアクション`pharmacistNames`**を新設（氏名のみ返す、登録番号等は含まない）。`edit-daily.js`が、この認証済みエンドポイントから動的に氏名一覧を取得してプルダウンを生成する方式に変更。これでGitHub上の公開コードには薬剤師の個人名が一切残っていない状態になった

5. **「進行状況メモが古い」** → このメモ自体を更新することで対応（このファイルが最新版）

---

## 未着手：⑤⑥

- ⑤ 操作履歴（誰が何を変更したかの記録）
- ⑥ 連続アクセス制限・同時保存時の競合対策

---

## 現在も継続中の暫定措置（薬剤師名簿の全体的な非公開）

薬剤師名簿の**一覧閲覧・保存・削除機能そのもの**（`status.html`のステータス画面での一覧表示、`edit-status.html`での編集）は、Googleログインが実装される前の暫定措置として、GAS側で完全にブロックしたままになっている（`pharmacistsHidden`フラグ、`saveStatusPharmacist`/`deleteStatusPharmacist`のルーティング拒否）。

Googleログインが実装された今、この措置を維持するか解除するかは**まだユーザーと合意していない**。次に対応する際は、「認証がかかった以上、暫定措置は解除してよいか」をユーザーに確認してから進めること。氏名の個別取得（`pharmacistNames`アクション）はこの暫定措置とは別枠で、④の対応の一環として新設・稼働中。

---

## 動作確認の状況

Googleログイン自体（画面表示・サインイン・他ページへの遷移・保存・未ログイン時のブロック）は、ユーザー側で一通り確認済みで「概ねOK」の評価を得ている。今回の追加修正（①のURL露出解消、②のキャッシュ、③の厳格化、④の薬剤師名動的取得）は、**まだユーザー側での実機確認が済んでいない**。次にこの作業を引き継ぐ場合は、まずこれらの再確認を依頼すること。

---

以上。次の一手は、①薬剤師名簿の暫定ブロックを解除するかどうかの確認、②⑤⑥（操作履歴・連続アクセス制限）への着手、のいずれかをユーザーと相談して決めること。
