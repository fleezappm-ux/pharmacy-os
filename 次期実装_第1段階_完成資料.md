# Pharmacy OS 次期実装 第1段階 完成資料（クロード→チャッピー・フリちゃん）

作成時点：2026年9月3日

「Pharmacy OS 次期実装仕様書」の**第1段階（利用者プロフィール・利用承認・権限管理）のみ**を実装しました。第2段階（確認印自動化・重複防止・LINE内ブラウザ対策）・第3段階（実績一覧・業務日誌）には着手していません。

事前に安定版一式を`backup_before_stage1`としてバックアップ済みです。

---

## 1. 変更ファイル一覧

| ファイル | 変更内容 |
|---|---|
| `code.gs` | 利用者DB（Usersシート）を氏名・役職・所属店舗・個別権限に対応する形へ拡張。約20の保存・削除処理すべてに権限チェックを追加 |
| `invite.html`／`invite.js` | Googleログイン後、氏名・役職・所属店舗を入力するプロフィールフォームを追加 |
| `admin.html`／`admin.js` | 利用者一覧に、氏名・役職・所属店舗の編集欄と、3つの編集権限のチェックボックスを追加 |
| `auth.js` | ログイン後に権限を確認し、権限が1つもなければ下部ナビの「編集」を非表示にする処理を追加 |
| `edit.html` | 権限のある分類（日次業務／月次業務／ステータス）のカードだけを表示するよう変更 |
| `home.html`・`index.html`・`monthly.html`・`insurance.html`・`status.html`・`yearly.html`・`edit-daily.html`・`edit-status.html`・`edit-monthly.html` | 下部ナビの「編集」リンクに、表示制御用の共通ID（`nav-edit-link`）を付与（見た目の変更はなし） |

---

## 2. 設計の要点

### 利用者DB（Usersシート）の拡張

既存の列に加えて、以下を追加しました。

`name`（氏名）／`store`（所属店舗）／`canEditDaily`（日次業務編集権限）／`canEditMonthly`（月次業務編集権限）／`canEditOther`（ステータス編集権限）／`canManageUsers`（利用者管理権限）

`role`列は、これまでの`admin`／`employee`という2択から、**役職コード**（`system_admin`／`owner`／`managing_pharmacist`／`pharmacist`／`clerk`／`other`）を直接保持する方式に変更しました。既存の`admin`という値は、互換性のためそのまま「最高権限」として扱われます（実際の書き換えは行っていません）。

### 即時登録＋ゼロ権限方式（フリちゃんの指示による簡略化）

仕様書では「確認待ち」という専用の利用不可状態を新設する設計でしたが、**今回はより簡易な方式**を採用しました。

- 招待URLから登録すると、**その場ですぐに利用者として登録される**（仕様書のような「確認待ち専用画面」は作っていません）
- ただし、**編集権限は3つとも自動的にfalse（オフ）**で登録される
- 権限がすべてオフの状態では、下部ナビの「編集」自体が表示されず、GAS側でも保存・削除系の操作がすべて拒否される
- 結果として、**フリちゃんが管理画面で個別に権限をオンにするまでは「見るだけ」の状態**になり、仕様書が意図していた「承認待ち」と実質的に同じ効果を、より少ない変更で実現しています

「日次記録の新規入力」と「既存記録の編集」は、仕様書では別権限として書かれていましたが、指示通り**`canEditDaily`1つに統合**しています。

### 役職の選択肢

自己申告フォームでは、`system_admin`（SE兼任管理薬剤師＝フリちゃん専用）は選択肢に表示されません。選べるのは「開設者」「管理薬剤師」「薬剤師」「事務」「その他」の5つです。

### GAS側の権限確認

`doPost`のルーティング部分に、**アクション名→必要な権限**の対応表（`EDIT_PERMISSION_MAP`）を1つ用意し、実際の保存・削除処理を呼び出す前に一括でチェックする設計にしました。これにより、既存の各保存・削除関数の中身には一切手を加えずに、全箇所へ確実に権限確認をかけることができています。フロント側が編集ボタンを隠していても、GASへ直接リクエストを送った場合は、この一括チェックで拒否されます。

### `canEditOther`の対応先

既存の「ステータス」編集グループ（届出・許可登録・薬剤師名簿・登録販売者名簿・基本情報・試験検査機関）に対応させました。

### システム管理者（フリちゃん）の保護

`role`が`system_admin`（または旧`admin`）の利用者は、

- 常に全権限（4つとも）を持つ
- 他の利用者から一時停止・削除・プロフィール変更・権限変更が一切できない（GAS側の全管理者関数で拒否）

という扱いにしています。

### 所属店舗について

事前確認どおり、**単一店舗のままデータへの店舗タグ付けは行っていません**。「所属店舗」はプロフィール項目として保存するだけで、日次記録や月次実績データそのものには店舗の区別を持たせていません。

---

## 3. 追加のスクリプトプロパティ

**新規追加はありません。** 既存のスクリプトプロパティ（`GOOGLE_CLIENT_ID`・`ALLOWED_EMAILS`・`USER_DB_SPREADSHEET_ID`・`EMPLOYEE_SLOT_LIMIT`・`LINE_CHANNEL_ACCESS_TOKEN`など）のみで動作します。

---

## 4. 実機確認手順

| # | 確認内容 | 手順 |
|---|---|---|
| 1 | 未登録者が氏名・役職・所属店舗を自己申告できる | 新しい招待URLを発行し、Googleログイン後にプロフィールフォームが出るか確認 |
| 2 | 登録直後は権限ゼロで、アプリを見るだけの状態になる | 登録直後のアカウントでログインし、「編集」が下部ナビに出ない・保存操作ができないことを確認 |
| 3 | SE兼任管理薬剤師（フリちゃん）が登録内容を修正できる | 管理画面の利用者一覧で、氏名・役職・所属店舗を書き換えて保存できるか確認 |
| 4 | SE兼任管理薬剤師が権限を付与できる | 管理画面で「日次業務編集」等のチェックを入れて保存し、その利用者側で編集が使えるようになるか確認 |
| 5 | 薬剤師には日次編集メニューが表示される | 「日次業務編集」だけを付与した利用者でログインし、編集の下部ナビが表示され、日次業務のカードだけ選べるか確認 |
| 6 | 事務・その他には現在の編集メニューが表示されない（権限なしの場合） | 権限を何も付与していない利用者で、編集ナビが出ないことを確認 |
| 7 | 管理薬剤師は利用者管理画面を利用できない | `canEditDaily`等は持つが`system_admin`ではない利用者で`admin.html`を開き、「管理者のみ利用できます」と出るか確認 |
| 8 | GASへの直接アクセスでも権限なしの更新・削除を拒否できる | （チャッピー側で、権限を持たないトークンから直接`updateDailyReport`等を呼んで拒否されるか確認） |
| 9 | 現在の招待URL機能が壊れていない | グループ招待・個別招待とも、これまで通り発行・使用できるか確認（プロフィール入力が増えた点以外は同じ動きです） |

---

## 5. 今回変更していないもの（指示通り）

- 確認印の自動化（第2段階）
- 日次記録の重複防止（第2段階）
- LINE内ブラウザ対策の強化（第2段階、現状の簡易検知のみ維持）
- 実績一覧・業務日誌（第3段階）
- 下部ナビゲーションの構成
- 既存のNotion DB構造
- LINE Broadcast通知（トークン未設定なら何も送らない状態のまま、今回も有効化していません）
- 薬剤師名簿全体の暫定ブロック解除（すでに解除済みの状態を維持、今回は追加の変更なし）

---

第1段階の実機確認が終わり、問題がなければ、第2段階に進みます。
