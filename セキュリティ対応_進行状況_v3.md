# Pharmacy OS セキュリティ対応 進行状況（クロード→チャッピー）※v3

対象：あおい薬局「Pharmacy OS」アプリのセキュリティ強化作業
作成時点：2026年9月2日（v2からの更新版）

**このメモは「セキュリティ対応_進行状況_v2.md」を上書きする最新版です。今回、文書②で指摘された4点（ステップ1）への対応が完了しました。**

---

## 経緯

チャッピーの再レビュー（文書②）で、以下4点の追加修正指示を受けました。同時に文書③（利用者招待・管理機能）も届きましたが、**規模とリスクの観点から②→③の順で分割して進める方針**を確認済みです（詳細は「追加指示への対応方針.md」参照）。今回はステップ1（文書②の4点）が完了した報告です。**文書③（招待機能）にはまだ着手していません。**

---

## 文書②：4点の追加修正（すべて完了）

### 1. 通常の日次入力画面（`script.js`）の確認印

**指摘**：`edit-daily.js`は認証後に`pharmacistNames`を取得する方式に変更済みだが、通常の日次入力`script.js`は削除済みの`PHARMACY_CONFIG.PHARMACISTS`をまだ参照していた。

**対応**：`script.js`にも`loadPharmacistNames()`関数を追加し、`edit-daily.js`と同じ方式（ログイン後に認証済みエンドポイント`pharmacistNames`から動的取得）に統一。

### 2. 認証キャッシュをトークン期限内に制限

**指摘**：GAS側の認証結果キャッシュが一律300秒固定で、IDトークン自体の残り有効期限が300秒未満の場合、トークン期限切れ後もキャッシュが有効なまま残る可能性があった。

**対応**：`verifyIdToken`のキャッシュ保存時に、**「300秒」と「トークンの残り有効期限」の短い方**をTTLとして使うよう変更。さらに、**キャッシュ読込時にも**、キャッシュに保存しておいたトークンの有効期限（`exp`）を見て、期限切れなら即座にキャッシュを破棄し再検証する処理を追加（Google側のtokeninfo再問い合わせが発生する）。これにより、許可メール一覧から外された利用者や期限切れトークンが、キャッシュ経由で必要以上に長く使えてしまう状態を防止。

### 3. 枚数・回数の整数限定（GAS側）

**指摘**：処方箋枚数・受付回数・件数・回数など整数であるべき項目に、小数やマイナス値を防ぐチェックがなかった。

**対応**：`sanitizeNumber`関数に`integer`オプションを追加し（`Number.isInteger()`チェック、非整数はエラーで保存を拒否）、以下の項目に適用：

- 日次業務の「処方箋枚数」（新規・編集とも）
- 処方箋調べの「処方箋受付回数」「歯科／眼科／耳鼻咽喉科／その他（処方箋受付枚数）」
- 処方箋集中率の「受付回数」
- 保険調剤実績の「件数（件）」「処方箋回数_医科」「処方箋回数_歯科」「処方箋回数_合計」
- 在宅患者管理の「件数（件）」「回数（回）」

**整数化しなかった項目**（指摘通り、小数を許容）：後発品調剤率の各割合（新指標割合・カットオフ値割合）、処方箋集中率の「全体割合」、保険調剤実績の「調剤報酬点数_合計」。

### 4. ログイントークンの自動更新

**指摘**：約1時間ごとの手動再ログインを避けたい。安全な範囲での自動更新＋失敗時のみ再ログイン、という方式を希望。

**対応**：`auth.js`を全面的に拡張し、以下の仕組みを実装。

- **静かな自動更新**：ログイン成功のたびに、トークン期限の5分前を狙ってタイマーをセットし、`google.accounts.id.prompt()`（GISのOne Tap/自動選択の仕組み）を1回だけ試みる。Googleのログインセッションが有効なら、画面を出さずに新しいトークンを取得できる
- **失敗時は無理に突破しない**：ブラウザ制限（FedCM無効化、サードパーティCookieブロック等）で自動更新が表示・成功しなかった場合は、その場でログイン画面を出したりはせず、静かに諦める。次にGASへアクセスするタイミングで、`authFetch`が認証エラーを検知し、その時点で初めてログイン画面を表示する
- **無限再試行・連続表示の防止**：自動更新の試行は1回きり（`silentRefreshInFlight`フラグで多重実行を防止）。失敗しても、ログイン画面の連続表示や無限ループは起きない設計
- **保存中の入力内容を失わない**：共通関数`authFetch`を、認証エラー時に「ログイン画面を出し、ログイン成功後に**同じリクエスト（保存内容含む）を自動的に再送**する」方式に変更。これにより、保存操作の途中でトークンが切れても、入力し直す必要がない
- **適用範囲**：日次業務（`script.js`の`sendDailyRecord`）、保険調剤実績・在宅患者管理（`insurance.js`）、月次業務のCSV取込・処方箋調べ保存（`monthly.js`）を含め、**GASへの全通信をこの仕組みに統一**。個別に`fetch`を直書きしていた箇所はすべて`authFetch`（または同等のリトライロジック）経由に置き換え済み
- **永久ログインにはしない**：自動更新はGoogleの正規ログイン状態（セッション有効性）に依存する形にとどめ、独自のトークン発行や有効期限延長の仕組みは作っていない。許可メール一覧（`ALLOWED_EMAILS`）のチェックも、更新のたびに毎回サーバー側で再実行される

---

## この段階での既存の変更なし事項（維持）

- `ALLOWED_EMAILS`方式は変更していない
- 薬剤師名簿全体の一覧・編集の暫定ブロックは、**招待機能と実機確認が完了するまで維持**（今回は解除していない、以前解除済みの状態のまま）
- 日次・月次・年度実績・ステータスの既存ロジックには手を入れていない

---

## 次のステップ

文書②（ステップ1）の実機確認をお願いします。確認項目は以下の通りです。

1. 通常の日次入力で確認印を選択できる
2. 日次編集でも確認印を選択できる
3. ログイントークンがURLに一切入らない（前回から継続）
4. ログイン済みでページ移動しても通常利用できる
5. トークン更新成功時は作業を継続できる（ブラウザを開いたまま1時間程度様子を見るか、ブラウザの開発者ツールでトークンの`exp`を書き換えて早める等で検証可能）
6. 更新失敗時は認証なしで処理せず、ログイン画面へ戻る
7. 未許可メールでは読み取り・保存・更新・削除できない
8. 枚数・回数へ小数を送ると保存を拒否する（例：処方箋枚数に「1.5」を試す）
9. 既存の日次・月次・ステータス機能を壊していない

実機確認が完了し、チャッピーの再レビューでも問題なければ、**ステップ2（文書③：利用者招待・管理機能）に着手します。**
